{
  "name": "cloudflare-astro-blog",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami"
  },
  "dependencies": {
    "@astrojs/mdx": "latest",
    "astro": "latest",
    "typescript": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
